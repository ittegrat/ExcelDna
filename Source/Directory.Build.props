<Project>

	<Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../../'))" Condition="exists($([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../../')))"/>

	<PropertyGroup>

		<Product>Excel-DNA Add-In Framework for Microsoft Excel</Product>
		<Copyright>Copyright © 2005-2022 Govert van Drimmelen</Copyright>
		<!-- If pre-release, 'revision' is pre-release * 1000 -->
		<Version>1.7.0.5000</Version>
		<FileVersion>1.7.0.5000</FileVersion>
		<InformationalVersion>1.7.0.0-rc5 (ittegrat)</InformationalVersion>

		<LibDistFolder>$(SolutionDir)..\_libs_</LibDistFolder>

		<EnableUnitySupport>true</EnableUnitySupport>
	</PropertyGroup>
</Project>
